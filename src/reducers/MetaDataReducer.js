import * as ActionConstants from '../constants/ActionConstants';

var defaultState = {
    xAuthToken: null
}


export default (state = defaultState, action) => {
    switch (action.type) {
        default:
            return state
    }
}